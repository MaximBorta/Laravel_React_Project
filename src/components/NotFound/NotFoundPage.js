import React from 'react'
import {Typography} from "@mui/material";

export const NotFoundPage = () => {
    return (
        <div>
            <Typography variant={'h3'} color={'secondary'}>Not Found... :(</Typography>
        </div>
    )
}